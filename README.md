# elements

Combine elements to win.

## Build

Make sure you have installed:
- CMAKE >3.24 
- C++20 compiler

Run:
```bash
cmake -B build
cmake --build build
```

output executable will be located at `build/app/game/Elements-*build information*`

## Run

to run Elements, make sure you have the elements_config.txt in the same directory.